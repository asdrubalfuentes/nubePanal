module.exports = {
    port: process.env.WEBSOCKET_PORT || 3001,
    options: {
      // Opciones específicas de WebSocket, si las hay
    },
    // Cualquier otra configuración global que pueda ser necesaria para tus WebSockets
  };
  